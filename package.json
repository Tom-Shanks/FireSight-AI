{
  "name": "firesight-ai",
  "version": "1.0.0",
  "description": "FireSight AI - Wildfire Prediction and Prevention System",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start",
    "test": "cd frontend && npm test",
    "install:api": "cd api && npm install",
    "postinstall": "npm run install:api"
  },
  "workspaces": [
    "frontend",
    "api"
  ],
  "keywords": [
    "wildfire",
    "prediction",
    "machine learning",
    "risk assessment"
  ],
  "author": "FireSight AI Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Tom-Shanks/FireSight-AI.git"
  },
  "devDependencies": {
    "serverless": "^3.30.1",
    "serverless-python-requirements": "^6.0.0",
    "serverless-prune-plugin": "^2.0.2",
    "serverless-api-gateway-throttling": "^2.0.3"
  }
} 